# CLAUDE.md

> **⚠️ Starter template.** This repository is currently **empty** — no source
> code, commits, or branches existed when this file was generated. The sections
> below are a scaffold with `TODO` placeholders. Replace each placeholder with
> real details as the codebase takes shape. **Do not treat any placeholder as a
> factual description of the project.** When code lands, an AI assistant should
> regenerate this file by actually inspecting the repository.

This file gives guidance to AI assistants (Claude Code and others) working in
this repository.

---

## Project Overview

<!-- TODO: One or two paragraphs describing what this project is and does. -->

- **Purpose:** _TODO — what problem does this app solve?_
- **Status:** New / empty repository (as of this template's creation).

> Note: The repository is named **Pump-App**, which may hint at a
> pump.fun-style crypto/trading application — but this is an **unverified
> guess**. Confirm the actual domain before documenting it here.

---

## Tech Stack

<!-- TODO: Fill in once dependencies exist. Delete rows that don't apply. -->

| Area              | Choice        |
| ----------------- | ------------- |
| Language          | _TODO_        |
| Runtime           | _TODO_        |
| Framework         | _TODO_        |
| Package manager   | _TODO_        |
| Styling           | _TODO_        |
| Testing           | _TODO_        |
| Lint / Format     | _TODO_        |

---

## Repository Structure

<!-- TODO: Replace with the real directory tree and a one-line note per dir. -->

```
.
├── .claude/          # Claude Code local settings (present today)
└── CLAUDE.md         # This file
```

_TODO: document top-level directories (e.g. `src/`, `app/`, `packages/`,
`tests/`) and what each contains._

---

## Development Workflow

<!-- TODO: Fill in the real commands from package.json / Makefile / CI. Do NOT
     invent commands — verify they exist before documenting or running them. -->

```bash
# Install dependencies
# TODO

# Start the dev server
# TODO

# Build for production
# TODO

# Run tests
# TODO

# Lint / format
# TODO
```

---

## Architecture & Key Concepts

<!-- TODO: Describe the high-level architecture once code exists. -->

- **Application structure:** _TODO (how features/modules are organized)_
- **State management:** _TODO_
- **Data / API layer:** _TODO (backends, blockchain/web3 integration, etc.)_
- **Domain terminology:** _TODO (glossary of project-specific terms)_

---

## Conventions

<!-- TODO: Capture the conventions the codebase actually adopts. -->

- **Naming:** _TODO (files, components, variables)_
- **Import aliases:** _TODO (e.g. `@/` → `src/`)_
- **Styling approach:** _TODO_
- **Directory layout rules:** _TODO_

---

## Testing

<!-- TODO: Document the framework, where tests live, and how to run them. -->

- **Framework:** _TODO_
- **Location:** _TODO (e.g. co-located `*.test.ts` vs `tests/` directory)_
- **Run command:** _TODO_

---

## Git & Contribution Workflow

The following reflects how work has been set up so far:

- **Default branch:** _TODO (none exists on the remote yet)_
- **Feature branches** observed use the `claude/<topic>` naming pattern
  (e.g. `claude/claude-md-docs-xpffs6`).
- **Push:** `git push -u origin <branch-name>`.
- **Pull requests:** Open a PR against the default branch once it exists. Check
  for a PR template under `.github/` and follow it if present.
- **Commits:** Write clear, descriptive commit messages.

---

## Notes for AI Assistants

- This is a **template**. Before relying on any section above, verify it against
  the actual repository — much of it is still `TODO`.
- **Never fabricate** commands, paths, or architecture. Confirm commands exist
  in `package.json` (or the relevant config) before running them.
- Prefer reusing existing patterns and utilities over introducing new ones.
- **Keep this file current:** when you add meaningful structure (dependencies,
  directories, scripts, tests), update the corresponding section here and remove
  the `TODO` markers.
